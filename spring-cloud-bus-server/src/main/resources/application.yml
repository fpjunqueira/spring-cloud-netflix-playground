---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/fpjunqueira/spring-cloud-netflix-playground
          searchPaths: spring-cloud-bus-server/config
server:
  port: 8002